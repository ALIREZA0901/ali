<UserControl x:Class="Umbra.Views.VpnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <StackPanel>
    <TextBlock Text="VPN Manager" FontSize="20" FontWeight="SemiBold" />
    <TextBlock Text="Cores and profiles require confirmation before starting." Foreground="{StaticResource Brush.Muted}" Margin="0,6,0,16" />

    <WrapPanel Margin="0,0,0,12">
      <Button Content="Start VPN" Command="{Binding StartVpnCommand}" />
      <Button Content="Stop VPN" Command="{Binding StopVpnCommand}" Background="#3A3F52" Margin="8,0,0,0" />
      <Button Content="Update Core" Command="{Binding UpdateCoreCommand}" Background="#3A3F52" Margin="8,0,0,0" />
    </WrapPanel>

    <Border Background="{StaticResource Brush.Surface}" CornerRadius="8" Padding="12">
      <StackPanel>
        <TextBlock Text="Detected configs" FontWeight="SemiBold" />
        <TextBlock Text="Auto-detection of sing-box, OpenVPN, OpenConnect configurations." Foreground="{StaticResource Brush.Muted}" />
        <TextBlock Text="Status: Core not installed" Margin="0,8,0,0" />
      </StackPanel>
    </Border>
  </StackPanel>
</UserControl>
